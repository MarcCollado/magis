const PollDetails = {
  voted: {
    width: '100%',
    height: 30,
    backgroundColor: '#E8EAF6',
    textAlign: 'center',
  },
  container: {
    display: 'flex',
    justifyContent: 'center',
  },
  options: {
    marginTop: 20,
    marginBottom: 10,
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
};

const UserCard = {
  card: {
    marginTop: 15,
  },
  media: {
    height: 0,
    paddingTop: '56.25%',
  },
};

const Feed = {
  container: {
    width: '85%',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  feed: {
    padding: 0,
    width: '100%',
    maxWidth: 480,
  },
};

const FourOFour = {
  container: {
    width: '75%',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  feed: {
    padding: 0,
    width: '100%',
    maxWidth: 480,
  },
};

const Leaderboard = {
  container: {
    width: '75%',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  feed: {
    padding: 0,
    width: '100%',
    maxWidth: 480,
  },
};

const Login = {
  container: {
    width: '75%',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  feed: {
    padding: 0,
    width: '100%',
    maxWidth: 480,
  },
};

const Nav = {
  root: {
    flexGrow: 1,
  },
  flex: {
    flex: 1,
  },
  menuButton: {
    marginLeft: -12,
    marginRight: 20,
  },
};

const QuestionPage = {
  container: {
    width: '85%',
    margin: 'auto',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  feed: {
    marginTop: 15,
    padding: 0,
    width: '100%',
    maxWidth: 480,
  },
};

export {
  PollDetails,
  UserCard,
  Feed,
  FourOFour,
  Leaderboard,
  Login,
  Nav,
  QuestionPage,
};
